// Scaffold smoke test — verifies the test runner is wired up correctly.
// Replace with real tests in subsequent tasks.
import { describe, it, expect } from 'vitest';

describe('scaffold', () => {
  it('test runner is working', () => {
    expect(true).toBe(true);
  });
});
